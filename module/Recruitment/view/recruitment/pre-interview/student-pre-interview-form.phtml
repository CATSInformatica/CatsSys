<section class="content">
    
    <h3 class="text-center"><?php echo $message; ?></h3>
    <?php if ($form !== null): ?>
    <div class="row">
        <div class="col-xs-12 col-md-10 col-md-offset-1">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Dados de Inscrição</h3>
                    <div class="box-tools pull-right">
                        <button type="button" class="btn btn-box-tool" data-widget="collapse">
                            <i class="fa fa-minus"></i>
                        </button>
                    </div>
                </div>

                <div class="box-body">
                    <?php if ($registration != null): ?>
                        <div class="form-group">
                            <label class="control-label">Número de inscrição: </label>
                            <?php echo $registration->getRegistrationNumber(); ?>
                            <br>
                            <label class="control-label">Data de inscrição: </label>
                            <?php echo $registration->getRegistrationDate(); ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
            
                <?php
                $form->prepare();
                echo $this->form()->openTag($form);

                $regForm = $form->get('registration');
                $personForm = $regForm->get('person');
                $preInForm = $regForm->get('preInterview');

                echo $this->partial('person/template', array(
                    'form' => $personForm,
                ));

                echo $this->partial('disclosure/template', array(
                    'form' => $regForm,
                ));

                echo $this->partial('pre-interview/template', array(
                    'form' => $preInForm,
                ));

                ?>

                <div class="form-group">
                    <div class="col-xs-12 col-md-12 text-justify">
                        <?php echo $this->formRow($form->get('preInterviewConsent')); ?>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-xs-12 col-md-12">
                        <?php echo $this->formRow($form->get('submit')); ?>
                    </div>
                </div>

                <?php echo $this->form()->closeTag(); ?>
        </div>
    </div>
    <?php endif; ?>
</section>

<script>

    appConfig = (function () {
        var scriptSrc = 'app/pages/recruitment/pre-interview/pre-interview';

        return {
            getScriptSrc: function () {
                return scriptSrc;
            }
        };

    })();

</script>