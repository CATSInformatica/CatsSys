

<div class="container">
    <h1>Role index</h1>
    <hr>

    <?php if (!empty($this->error)): ?>
        <?php echo $this->error; ?>
    <?php else: ?>

        <div class="table-responsive">
            <table class="table table-condensed">
                <thead>
                    <tr>
                        <th class="text-center">#</th>
                        <th class="text-center">Name</th>
                        <th class="text-center">Parents</th>
                        <th class="text-center">Children</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($this->roles as $role): ?>
                        <tr>
                            <td class="text-center"><?php echo $this->escapeHtml($role->getRoleId()); ?></td>
                            <td class="text-center"><?php echo $this->escapeHtml($role->getRoleName()); ?></td>
                            <td class="text-center">
                                <div class="btn-toolbar" role="toolbar" aria-label="...">
                                    <?php foreach ($role->getParents()->toArray() as $parentRole): ?>
                                        <button type="button" class="btn btn-info"><?php echo $this->escapeHtml($parentRole->getRoleName()); ?></button>
                                    <?php endforeach; ?>
                                </div>
                            </td>
                            <td class="text-center">
                                <div class="btn-toolbar" role="toolbar" aria-label="...">
                                    <?php foreach ($role->getChildren()->toArray() as $childRole): ?>
                                        <button type="button" class="btn btn-info"><?php echo $this->escapeHtml($childRole->getRoleName()); ?></button>
                                    <?php endforeach; ?>
                                </div>
                            </td>
                            <td class="text-center"><a class="btn btn-danger"  href='<?php echo $this->url('authorization/role', array('controller' => 'role', 'action' => 'edit', 'id' => $role->getRoleId())); ?>'>Edit</a></td>
                            <td class="text-center"><a class="btn btn-danger" href='<?php echo $this->url('authorization/role', array('action' => 'delete', 'id' => $role->getRoleId())); ?>'>Delete</a></td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    <?php endif; ?>
</div>



