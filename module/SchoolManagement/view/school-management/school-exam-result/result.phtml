<section class="content">
    <div class="box box-success" id="definitions">
        <div class="box-header with-border">
            <h3 class="box-title"><i class="fa fa-cogs"></i> Definições</h3>
        </div>
        <div class="box-body">
            <div class="row">
                <div class="col-md-8 col-md-offset-2 col-xs-12">
                    <div class="col-md-6 col-xs-12">
                        <div class="form-group">
                            <label>Aplicação</label>
                            <select id="application" class="form-control">
                                <?php foreach ($apps as $app) : ?>
                                    <?php
                                    $s = $app->getStudentClass();
                                    $r = $app->getRecruitment();

                                    $sufix = '';

                                    if ($s) {
                                        $sufix .= ' | ' . $s->getClassName();
                                    }
                                    if ($r) {
                                        $sufix .= ' | ' . $r->getRecruitmentYn();
                                    }

                                    ?>
                                    <option value="<?php echo $app->getExamApplicationId(); ?>">
                                        <?php echo $app->getName() . $sufix;  ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-6 col-xs-12">
                        <div class="form-group">
                            <label>Simulado ou Processo Seletivo?</label>
                            <select id="classExamOrRecruitmentExam" class="form-control">
                                <option value="" selected disabled>Simulado ou PSA</option>
                                <option value="simulado">Simulado</option>
                                <option value="psa">Processo Seletivo</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="form-group" id="simuladoSelect" style="display:none">
                            <label>Turmas</label>
                            <select id="examClass" class="form-control">
                                <?php foreach ($classes as $cl) : ?>
                                    <option value="<?php echo $cl->getClassId(); ?>"> <?php echo $cl->getClassName(); ?></option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="form-group" id="psaSelect" style="display:none">
                            <label>Processo Seletivo de Alunos</label>
                            <select id="recruitment" class="form-control">
                                <option value="<?php echo $rec['id']; ?>"> <?php echo $rec['desc']; ?>
                                </option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="col-md-8 col-md-offset-2 col-xs-12">
                    <div class="col-md-6">
                        <button id="fetch-new-data" class="btn btn-primary btn-flat btn-block" disabled> <i class="fa fa-magic"></i> Carregar </button>
                    </div>
                    <div class="col-md-6">
                        <button id="fetch-current-data" class="btn btn-default btn-flat btn-block" disabled> <i class="fa fa-magic"></i> Resultado Salvo </button>
                    </div>
                </div>
            </div>
        </div>
        <!-- /.box-body -->
    </div>

    <div class="box box-success" id="definitions">
        <div class="box-header with-border">
            <h3 class="box-title"><i class="fa fa-columns"></i> Critérios</h3>
            <span>Defina os critérios de desempate</span>
        </div>
        <div class="box-body">
            <div class="row">
                <div class="col-md-10 col-md-offset-1 col-xs-12">
                    <div id="sort-group-container">

                    </div>

                    <div class="col-md-12">
                        <button id="set-criteria" class="btn btn-primary btn-flat btn-block" disabled>Definir</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="box box-success" id="definitions">
        <div class="box-header with-border">
            <h3 class="box-title"><i class="fa fa-columns"></i> Resultado</h3>
            <span>Clique no botão salvar para salvar a classificação da aplicação de prova</span>
        </div>
        <div class="box-body">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <button id="save-result" class="btn btn-app" disabled>
                        <i class="fa fa-save"></i>
                        Salvar
                        <span class="badge bg-green" id="resultProgress">0/0</span>
                    </button>
                </div>
            </div>
            <div class="row">
                <div id="result-container" class="col-md-10 col-md-offset-1 col-xs-12">
                </div>
            </div>
        </div>
    </div>

</section>

<script>
    appConfig = (function() {
        var scriptSrc = 'app/pages/school-management/exam/ApplicationResult';
        return {
            getScriptSrc: function() {
                return scriptSrc;
            }
        };
    })();
</script>